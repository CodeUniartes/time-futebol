# Export do Figma Make (referência)

Protótipo gerado em 2026-09-24 a partir do briefing do site (SP4). **Não é o site.** Serve de referência de layout,
componentes e tokens. O site de produção nasce em `web/site` (SP4), reaproveitando o que for bom daqui.

- Revisão com o que precisa mudar: [`../../design-review-figma-make.md`](../../design-review-figma-make.md).
- Marca e regras de contraste: [`../../brand.md`](../../brand.md).
- Dados (`src/data/mockData.ts`), login, cadastro e o algoritmo do painel são **de exemplo**.
- Fora do export de propósito: `AGENTS.md`/`CLAUDE.md` do Figma (instruções do ambiente dele), `.figma/`, `.mise.toml`,
  `pnpm-lock.yaml` e os arquivos com o texto do briefing.
- Para abrir localmente: `pnpm install` e `pnpm dev` dentro desta pasta (precisa de Node 22+).
